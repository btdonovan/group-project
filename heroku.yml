setup:
  - "null"
  # addons:
  # - plan: heroku-postgresql:hobby-dev
  #   as: DATABASE

build:
  docker:
    web: weather-client/Dockerfile
    # api: weather-api/Dockerfile

run:
  web: npm start
  # api: npm start